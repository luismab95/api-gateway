export const enum CodeHttpEnum {
  ok = 200,
  created = 201,
  badRequest = 400,
  unAuthorized = 401,
  forbidden = 403,
  notFound = 404,
  internalServerError = 500,
  timeout = 504,
  rateLimit = 429,
}
